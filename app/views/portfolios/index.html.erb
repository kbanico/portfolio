<h1>Portfolio Items</h1>

<p><%= link_to "Create New Item",  new_portfolio_path %></p>


<% @portfolio_items.each do |item| %>
  <p><%= item.title %></p>
  <p><%= item.subtitle %></p>
  <%= image_tag(item.thumb_image) unless !item.thumb_image %>
  <%= link_to "Edit", edit_portfolio_path(item) %>
<% end %>
